<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <%- include("__bootstrap.ejs") %>
</head>
<body>
  <%- include("__header.ejs") %>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Event Details: <%= event.name %></h1>
    <div class="card">
      <% if (event.image) { %>
        <img src="<%= event.image %>" class="card-img-top" alt="<%= event.name %>" style="max-height: 300px; object-fit: cover;">
      <% } else { %>
        <div class="card-img-top bg-light text-center py-5">
          <p class="text-muted mb-0">No image available</p>
        </div>
      <% } %>
      <div class="card-body">
        <h5 class="card-title"><%= event.name %></h5>
        <p class="card-text"><strong>Date:</strong> <%= new Date(event.date).toLocaleString() %></p>
        <p class="card-text"><strong>Location:</strong> <%= event.location %></p>
        <p class="card-text"><strong>Category:</strong> <%= event.category %></p>
        <p class="card-text"><%= event.description || "No description available." %></p>
        <a href="/" class="btn btn-primary">Back to Home</a> <a href="/events/<%= event.id %>/book" class="btn btn-primary">Book Tickets</a>
      </div>
    </div>
  </div>
</body>
</html>